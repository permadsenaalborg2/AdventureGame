name: Docker Image CI

on:
  workflow_run:
    workflows: [".NET"]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag adventuregame
